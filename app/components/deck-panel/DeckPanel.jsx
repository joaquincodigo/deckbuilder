export default function DeckPanel() {
  const styles = {
    body: "w-screen h-full bg-amber-500",
  };

  return (
    <div className={styles.body}>
      <p className="text-white">I am Deck Panel</p>
    </div>
  );
}
