export default function AllCardsPanel() {
  const styles = {
    container: "bg-slate-300 z-30 w-12 h-12 fixed top-0 left-0",
  };

  return <div className={styles.container}>I am all cards panel</div>;
}
