export default function TabsBackground() {
  const styles = {
    TabsBackground: "fixed bottom-0 w-screen h-18 bg-black",
  };
  return (
    <div data-components="TabsBackground" className={styles.TabsBackground} />
  );
}
